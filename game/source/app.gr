import "level.gr"
import "menu.gr"

event app {
    debug();
    menu();
}

task debug() {
    loop yield {
        if(@Input.isDown(KeyButton.f5))
            @App.reload(true, true);

        if(@Input.isDown(KeyButton.escape))
            @App.close();
    }
}